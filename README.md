# deploy-me-packages

Public packages for deploy.me. Contains the TypeScript client SDK (published as `deploy.me`) and the `dp` CLI. The private control plane and marketing site live in the private `deploy-me` monorepo.
